type Mail {
  to: [String!]
  subject: String!
  body: String!
}

input NewMail {
  to: [String!]
  subject: String!
  body: Any!
}

extend type Mutation {
  send(tpl: String!, input: NewMail): Mail
  deleteMail(id: ID!): Mail
}

extend type Query {
  mail(id: ID!): Mail
  mails: [Mail]
}
